/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // enables static exports
  basePath: '/nafamubarokhusni.github.io', // replace with your repository name
  assetPrefix: '/nafamubarokhusni.github.io/', // replace with your repository name
};

module.exports = nextConfig;
